### 📌 Blog Classes Reference

this might be outdated but still its oke!

##


### General:
- article-container-normal → main container for each article
- article-title → big title of the blog
- article-date → publish date
- article-content → wraps all article text and elements
- article-footer → footer area with share buttons / last updated date

### Views:
- fa-solid fa-eye → views icon

### Headings:
- h2 → section titles
- h3 with <a class="anchor"> → sub-section with an anchor link

### Code Blocks:
- blog-code-block → wrapper for code sections
- code-header → top bar of the code box
- code-title → text "Code"
- copy-btn → copy button for code
- code-content → the actual code block
- language-bash / language-yaml → code highlighting types

### Notes / Features:
- feature-list → list with features or prerequisites
- feature-intro → intro text for the feature list
- note-block → whole note block
- note-block-header → header of the note
- note-block-icon → info icon
- note-block-title → title text
- note-block-content → content of the note

### Images:
- image-wrapper → wrapper for screenshots/images with Lightbox
- video-wrapper → wrapper for videos with Lightbox
- glightbox → video/image popup/lightbox class

### Other:
- bold → makes text bold (used inline)
- custom-title → add this attribute to set your own title instead of auto-sync